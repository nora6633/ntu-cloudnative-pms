# Copy to loadtest/.env and fill in. Never commit the real .env.
# loadtest/.env is gitignored at the repo root.

# Target — http://localhost:8080 for local backend, frontend URL for prod-like (e.g. Railway)
BASE_URL=

# Credentials of an employee-level account (not admin/HR)
PMS_USER=
PMS_PASS=
